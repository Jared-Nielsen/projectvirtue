# Dependencies
node_modules
.pnpm-store

# Build output
dist
build
.vite
.histoire
.turbo
.astro
storybook-static
*.tsbuildinfo

# Test coverage
coverage

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
Thumbs.db

# Env
.env
.env.local
.env.*.local

# Reference art kits (Kenney etc.) — kept dev-local; production art moves to
# Cloudflare R2 (media.gamecodex.com) with a sync script when real assets land.
# Re-download from kenney.nl while developing.
_tileart

# Interface art (HUD chrome, panel frames, button bitmaps, etc.) — same
# pipeline as _tileart: dev-local for prototyping, R2-hosted in production.
_interfaceart
