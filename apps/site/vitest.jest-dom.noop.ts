// Empty setup file. Its filename contains the substring "jest-dom" so that
// vite-plugin-solid's getJestDomExport() short-circuits and skips the
// auto-injection of '@testing-library/jest-dom/vitest', which is not a
// direct dep of @br/site and is not reliably resolvable from this app.
export {};
